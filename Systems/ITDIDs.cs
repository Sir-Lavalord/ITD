using ITD.Content.Projectiles.Friendly.Melee.Snaptraps;
using Terraria.ID;
using Terraria.ModLoader;
using static Terraria.ModLoader.ModContent;

namespace ITD
{
    public class ITDSets
    {
        //public static int[] IsASnaptrap; // this is kinda useless rn
        public static void Init()
        {
            /*
            IsASnaptrap = [
            ProjectileType<SniptrapProjectile>(),
            ProjectileType<SoulsnapperProjectile>(),
            ProjectileType<CrimeratrapProjectile>(),
            ProjectileType<VenusSnaptrapProjectile>(),
            ProjectileType<TheScorpionProjectile>(),
            ProjectileType<SnaptrapProjectile>(),
            ProjectileType<VocalZeroProjectile>(),
            ProjectileType<DespoticSnaptrapProjectile>(),
            ProjectileType<MrSnapkinsProjectile>()
            ];
            */
        }
    }
}